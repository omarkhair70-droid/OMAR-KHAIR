# SERAPH — CANONICAL FRAGMENT AUDIT

Status: **FRAGMENT LOCKED FOR SOURCE-FIRST INTEGRATION**
Date: 2026-09-23

Source repository:
`omarkhair70-droid/seraph`

Canonical source snapshot reviewed:
`main @ e49d37c0d7567f0bf780f033aa5f7311d290bc96`

Active experimental branch reviewed separately:
`lab/serara-interactive-presence-20260923`

Latest lab work is useful R&D, but it is **not** the source of truth for the exhibition until SERAPH itself accepts / merges those experiments.

---

# 1. DECISION

The exhibition does **not** make a new SERAPH.

The selected fragment is:

## **THE BODY — canonical chamber fragment**

Use the real canonical SERAPH body / chamber runtime from the source snapshot.

What enters the exhibition:
- canonical SERARA body asset;
- canonical chamber camera;
- canonical lighting;
- canonical material state;
- canonical VFX;
- canonical pointer / presence behaviour from the source snapshot;
- canonical stillness / tension / fall language already present in the source.

What does not enter automatically:
- WAKING RELIC;
- lab-only camera-perception experiments;
- lab-only diagnostic UI;
- proof telemetry;
- debug states;
- synthetic proof routes;
- every voice / narrative chapter simply because it exists in SERAPH.

The exhibition presents **THE BODY itself**, not a summary and not a remake.

---

# 2. WHY THIS FRAGMENT

SERAPH already answers the exhibition question without needing a new invented scene.

The current source already contains:
- a full humanoid presence;
- a cinematic chamber;
- authored camera drift;
- stateful posture;
- material response;
- sound architecture;
- rupture / aftermath language;
- a strong still frame.

The most truthful exhibition move is therefore to let the work remain itself.

The exhibition authors only:
- arrival from FIRST CONTACT;
- framing / orientation;
- global INDEX / CONTEXT access;
- sound-consent compatibility;
- exit to the next work.

---

# 3. SOURCE-FIRST BOUNDARY

## Copied / pinned from SERAPH source

The exhibition may pin exact source files from the reviewed commit under a provenance namespace.

Candidate source files:
- `components/seraph/SeraphWorld.tsx`
- `components/seraph/SeraraCanonicalBody.tsx`
- `components/seraph/CinematicChamberVFX.tsx`
- `components/seraph/serara-state.ts`
- `components/seraph/serara-material.ts`
- `components/seraph/serara-runtime-signal.ts`
- `components/seraph/serara-sonic.ts`
- `components/seraph/serara-presence-memory.ts`
- `components/seraph/serara-perception.ts`
- `components/seraph/serara-performance.ts`

Canonical binary asset:
- `public/assets/serara-canonical.glb`

Optional canonical audio only if the exhibition sound path enables it truthfully:
- `public/assets/serara-cinematic/audio/*`

## Exhibition-owned adapter

The exhibition adapter may change only:
- asset paths;
- import paths;
- sound consent wiring;
- route / transition callbacks;
- disposal boundaries;
- shell overlap;
- mobile / reduced-motion mounting policy where required for the exhibition container.

The adapter must not redesign:
- SERARA;
- body proportions;
- chamber art direction;
- material language;
- posture grammar;
- source interaction meaning.

---

# 4. LAB BRANCH RULE

The current SERARA lab branch contains valuable experiments:
- delayed recognition;
- stillness memory;
- semantic camera perception;
- embodied performance conductor;
- stronger camera-response readability.

Those experiments remain **SERAPH R&D**.

The exhibition must not silently adopt them before SERAPH itself declares them canonical.

If SERAPH later merges an accepted experiment:
1. record the new canonical source commit;
2. compare the exhibition fragment against it;
3. deliberately update the pinned fragment if useful.

No accidental drift.

---

# 5. ARRIVAL FROM FIRST CONTACT

FIRST CONTACT currently ends in a warm seam.

The SERAPH adapter may use that seam only as exhibition framing.

Transition:

```
FIRST CONTACT residue
      ↓
warm seam in darkness
      ↓
SERAPH chamber becomes visible behind it
      ↓
seam disappears
      ↓
THE BODY remains fully itself
```

The seam does not become a fake SERARA body.

No new lore is added.

---

# 6. EXPERIENCE INSIDE SERAPH

The visitor receives the canonical chamber.

No explanatory hero.

Minimal exhibition overlay:
- `SERAPH / THE BODY`
- `CONTEXT`
- `CONTINUE`

The overlay should recede quickly enough that the source work dominates.

The visitor may remain.

The exhibition must not force a timed exit.

---

# 7. EXIT

Leaving SERAPH is exhibition-authored.

The source chamber settles.

The exhibition captures one visual residue:
- topology / contour / seam / edge memory already visible in the source image.

That residue becomes the transition material into Fokhara.

The source work is not modified to "become pottery."

The transition begins only after the SERAPH fragment has settled.

---

# 8. SOUND

SERAPH source sound is meaningful, but the exhibition has a global sound decision.

Therefore:

If exhibition sound = ON:
- canonical SERAPH sonic system may wake on visitor interaction.

If exhibition sound = OFF:
- SERAPH must remain silent;
- no source listener may override the visitor's exhibition choice.

This is an integration adaptation, not an artistic redesign.

---

# 9. CAMERA / PERCEPTION

V1 exhibition fragment does **not** request webcam permission.

Reason:
- webcam perception is still active experimental R&D in the lab branch;
- the canonical fragment already works with pointer / presence logic;
- the exhibition should not create a permission event SERAPH itself has not canonically accepted.

When / if SERAPH merges camera perception canonically, the exhibition can revisit this boundary.

---

# 10. IMPLEMENTATION GATE

SERAPH exhibition integration closes only when:

- the exact canonical body asset is pinned from the recorded source commit;
- the chamber uses source-owned runtime code, not a visual imitation;
- no lab-only code leaks into V1;
- sound OFF remains truly silent;
- source code is disposed cleanly when leaving the room;
- desktop still frame matches the source identity;
- mobile framing remains recognizably the same work;
- INDEX / CONTEXT remain accessible without visually dominating the chamber;
- FIRST CONTACT handoff does not pretend to be part of SERAPH source;
- exit to Fokhara is finite and exhibition-owned;
- provenance lists every copied source file and commit;
- lint / typecheck / build pass;
- rendered browser proof is reviewed.

SERAPH_FRAGMENT = THE_BODY_CANONICAL_CHAMBER
SOURCE_SHA = e49d37c0d7567f0bf780f033aa5f7311d290bc96
NEXT = PIN_SOURCE_RUNTIME
