# FOKHARA — CANONICAL FRAGMENT AUDIT

Status: **FRAGMENT LOCKED FOR SOURCE-FIRST INTEGRATION**
Date: 2026-09-23

Source repository:
`omarkhair70-droid/Fokhara`

Canonical production snapshot:
`main @ f66bbd8c85bb6c4129e7e348ee6124cc56b6c665`

Canonical production deployment:
`VP10: Full visual review`

The source is visually accepted.

---

# 1. DECISION

The exhibition does **not** invent a new clay / material experience.

The selected fragment is:

## **HOME — OBJECT-FIRST THESIS**

The accepted Fokhara home first movement already carries the work:

- `THE FORM REMEMBERS`;
- real ceramic object first;
- mineral field;
- object mass + typographic counterweight;
- material-derived color;
- OWN / MAKE duality;
- restrained motion;
- no WebGL spectacle.

The exhibition presents that source movement as a pinned fragment.

---

# 2. WHY THIS FRAGMENT

VP10 explicitly accepted the Home surface as:

> object-first thesis and two clear OWN / MAKE paths

That is the smallest complete production fragment that communicates Fokhara without turning the exhibition room into a second Fokhara website.

The exhibition does **not** need:
- Shop grid;
- full Carry navigation;
- Collections;
- Cart;
- booking;
- Studio route;
- a new simulated clay world.

The full work remains available through `OPEN FULL WORK`.

---

# 3. SOURCE PRODUCTION LAW

Accepted production decisions that must survive:

- quiet mineral field;
- real Fokhara product media;
- material-derived accent;
- strong object / text counterweight;
- motion as finite consequence;
- no shader-driven material memory;
- no 3D carry theatre;
- no generic pottery texture;
- no fake clay deformation.

VP10 specifically rejected:
- Material Memory WebGL;
- full-screen inherited collection color wash;
- progress-like arrival bar;
- 3D carry theatre;
- full Kiln scroll experience.

The exhibition must not resurrect rejected experiments merely because the exhibition can use Three.js.

---

# 4. PINNED SOURCE FILES

Pin from the canonical source commit:

- `components/HomeObjectEntry.tsx`
- `components/ProductVisual.tsx`
- `lib/products.ts`
- `lib/visual/material-state.ts`
- `lib/visual/image-choreography.ts`
- `app/globals.css`
- `docs/visual/VP0_PRODUCT_MEDIA_SNAPSHOT.json`
- `docs/visual/VP10_FINAL_VISUAL_ACCEPTANCE.md`

The source stylesheet is pinned for provenance.

Only the selectors required by the fragment are namespaced into an exhibition adapter so Fokhara styles cannot overwrite the exhibition shell.

---

# 5. PRODUCT SNAPSHOT

For a deterministic exhibition render, the fragment uses a pinned real product state rather than making the exhibition depend on the live Woo API.

Selected object:

`Nebula Espresso Cup`

Source evidence:
- Woo id: `4848`;
- collection: `Nebula`;
- source snapshot status: `in_stock`;
- official product image:
  `https://fokharastudioandshop.com/wp-content/uploads/2025/12/dscf7409.webp`

The source product definition carries:
- price: EGP 300;
- form: espresso;
- source accent family.

This is not fabricated exhibition content.
It is a frozen source state used so the exhibition remains stable.

---

# 6. INTERACTION BOUNDARY

The original Home fragment contains links into Shop / Workshops and the product Carry system.

The exhibition does **not** copy the complete commerce navigation graph.

Therefore V1 adapts only navigation edges:

- `Explore ceramics` → opens the full Fokhara Shop;
- `Enter the process` → opens the full Fokhara Workshops;
- the object frame remains visually canonical but does not run a fake internal Carry to a non-existent exhibition Shop route.

This is deliberate subtraction, not a reimplementation.

The real Carry system remains in the full source work.

---

# 7. ARRIVAL FROM SERAPH

SERAPH leaves a body contour / line residue.

The exhibition transition flattens that contour into one quiet mineral trace.

Then the trace disappears behind the canonical Fokhara source composition.

No body becomes clay.
No fake morphing sculpture.

The source fragment appears as itself.

---

# 8. EXIT

Fokhara does not transform into Habba.

The exhibition owns the handoff:

- source fragment settles;
- one material-derived accent remains;
- the accent separates into small discrete points;
- the route moves to Habba.

The Fokhara source remains visually untouched.

---

# 9. IMPLEMENTATION GATE

Close only when:

- exact source files are pinned from `f66bbd8...`;
- real official product media is used;
- the first frame remains recognizably Fokhara production;
- typography / mineral field / object scale match source intent;
- no WebGL is introduced;
- internal commerce is not faked;
- full source work remains one click away;
- desktop/mobile remain authored;
- reduced motion remains complete;
- SERAPH arrival framing is clearly exhibition-owned;
- Habba exit framing is clearly exhibition-owned;
- no source CSS leaks into global exhibition styling;
- provenance records every adaptation;
- lint / typecheck / build pass;
- rendered browser proof is reviewed.

FOKHARA_FRAGMENT = HOME_OBJECT_FIRST_THESIS
SOURCE_SHA = f66bbd8c85bb6c4129e7e348ee6124cc56b6c665
NEXT = PIN_FOKHARA_SOURCE
